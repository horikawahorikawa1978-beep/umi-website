/* =========================================================
   NEWS — Umi corporate news / PR TIMES
   ========================================================= */

.navlinks .is-current{position:relative}
.navlinks .is-current:after{
  content:"";position:absolute;left:0;right:0;bottom:-12px;
  width:26px;height:1px;margin:auto;background:var(--gold)
}

/* HOME : LATEST NEWS */
.home-news{padding:72px 0 80px;background:#fff}
.home-news-inner{max-width:980px}
.home-news-heading{
  display:flex;align-items:flex-end;justify-content:space-between;
  gap:28px;margin-bottom:28px
}
.home-news-kicker{
  margin:0 0 10px;font-family:Arial,sans-serif;font-size:11px;
  font-weight:700;letter-spacing:.24em;color:var(--gold)
}
.home-news-heading h2{
  margin:0;font-size:28px;font-weight:400;letter-spacing:.05em;color:var(--deep)
}
.home-news-viewall{
  font-family:Arial,sans-serif;font-size:10px;font-weight:700;
  letter-spacing:.18em;color:var(--deep);white-space:nowrap
}
.home-news-viewall span{margin-left:8px}
.home-news-item{
  display:grid;grid-template-columns:110px 130px 1fr 26px;
  align-items:center;gap:22px;padding:26px 2px;
  border-top:1px solid var(--line);border-bottom:1px solid var(--line);
  transition:.2s
}
.home-news-item:hover{padding-left:10px;background:#fbfaf7}
.home-news-item time{
  font-family:Arial,sans-serif;font-size:11px;letter-spacing:.08em;color:#73869a
}
.home-news-category{
  display:inline-block;width:max-content;padding:6px 10px;border:1px solid #d5c39e;
  font-family:Arial,sans-serif;font-size:9px;letter-spacing:.12em;color:#9b762c
}
.home-news-item p{
  margin:0;font-size:15px;line-height:1.7;letter-spacing:.025em;color:var(--deep)
}
.home-news-arrow{font-family:Arial,sans-serif;color:var(--gold);text-align:right}

/* NEWS LIST */
.news-page,.news-detail-page{background:#fff;color:var(--deep)}
.news-hero{
  padding:86px 0 78px;
  background:linear-gradient(120deg,#f8f6f1 0%,#fff 58%,#f4f1f7 100%);
  border-bottom:1px solid rgba(6,36,74,.06)
}
.news-hero-inner{text-align:center}
.news-en{
  margin:0 0 10px;font-family:Georgia,"Times New Roman",serif;
  font-size:48px;letter-spacing:.08em;color:var(--deep)
}
.news-hero h1{margin:0;font-size:18px;font-weight:400;letter-spacing:.26em}
.news-gold-line{width:38px;height:1px;background:var(--gold);margin:27px auto}
.news-lead{margin:0;font-size:14px;letter-spacing:.08em;color:#647b92}

.news-list-section{padding:70px 0 94px}
.news-container{max-width:920px}
.news-filter{
  display:flex;gap:24px;flex-wrap:wrap;padding-bottom:22px;
  border-bottom:1px solid #e2e5e8;
  font-family:Arial,sans-serif;font-size:10px;font-weight:700;
  letter-spacing:.14em;color:#8a99a8
}
.news-filter span{padding-bottom:8px}
.news-filter .is-active{color:var(--deep);border-bottom:1px solid var(--gold)}
.news-card>a{
  display:block;padding:34px 8px 38px;border-bottom:1px solid #e2e5e8;
  transition:.2s
}
.news-card>a:hover{padding-left:16px;background:#fbfaf7}
.news-card-meta{
  display:flex;align-items:center;gap:18px;margin-bottom:18px;
  font-family:Arial,sans-serif
}
.news-card-meta time{font-size:11px;letter-spacing:.08em;color:#71859a}
.news-card-meta span{
  padding:5px 9px;border:1px solid #d5c39e;
  font-size:9px;letter-spacing:.12em;color:#9b762c
}
.news-card h2{
  margin:0 0 16px;font-size:21px;font-weight:400;line-height:1.75;
  letter-spacing:.02em;color:var(--deep)
}
.news-card p{margin:0;color:#60778e;font-size:14px;line-height:1.9}
.news-card-more{
  margin-top:20px;font-family:Arial,sans-serif;font-size:10px;
  font-weight:700;letter-spacing:.16em
}
.news-card-more b{margin-left:8px;color:var(--gold)}

/* NEWS DETAIL */
.news-detail{padding:82px 0 96px}
.news-detail-wrap{width:min(820px,calc(100% - 48px));margin:0 auto}
.news-detail-meta{
  display:flex;align-items:center;gap:18px;margin-bottom:24px;
  font-family:Arial,sans-serif
}
.news-detail-meta time{font-size:11px;letter-spacing:.08em;color:#71859a}
.news-detail-meta span{
  padding:5px 9px;border:1px solid #d5c39e;
  font-size:9px;letter-spacing:.12em;color:#9b762c
}
.news-detail h1{
  margin:0;font-size:clamp(25px,3vw,38px);font-weight:400;
  line-height:1.65;letter-spacing:.025em;color:var(--deep)
}
.news-detail-line{width:44px;height:1px;background:var(--gold);margin:32px 0 42px}
.news-detail-body p{
  margin:0 0 24px;font-size:15px;line-height:2.05;
  letter-spacing:.035em;color:#405f7d
}
.prtimes-button{
  display:inline-flex;align-items:center;gap:20px;margin-top:22px;
  padding:17px 25px;border:1px solid var(--deep);
  font-family:Arial,"Yu Gothic",sans-serif;font-size:11px;font-weight:700;
  letter-spacing:.11em;transition:.2s
}
.prtimes-button:hover{background:var(--deep);color:#fff}
.prtimes-button span{color:var(--gold)}
.news-detail-back{
  margin-top:64px;padding-top:24px;border-top:1px solid #e2e5e8;
  font-family:Arial,"Yu Gothic",sans-serif;font-size:11px;letter-spacing:.08em
}

/* NEWS mobile */
@media(max-width:800px){
  .navlinks .is-current:after{display:none}
  .home-news{padding:52px 0 58px}
  .home-news-heading{align-items:flex-start}
  .home-news-heading h2{font-size:23px}
  .home-news-item{
    grid-template-columns:92px 1fr 20px;gap:10px 14px;padding:22px 0
  }
  .home-news-category{grid-column:2}
  .home-news-item p{grid-column:1 / 3;font-size:14px}
  .home-news-arrow{grid-column:3;grid-row:1 / 4}
  .news-hero{padding:58px 0 52px}
  .news-en{font-size:38px}
  .news-list-section{padding:46px 0 64px}
  .news-filter{gap:14px}
  .news-card>a{padding:28px 0 32px}
  .news-card h2{font-size:18px}
  .news-detail{padding:52px 0 68px}
  .news-detail-wrap{width:calc(100% - 36px)}
  .news-detail h1{font-size:25px;line-height:1.7}
  .news-detail-body p{font-size:14px}
  .prtimes-button{width:100%;justify-content:space-between}
}
